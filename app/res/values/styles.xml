<resources>
    <style name="Binacty" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorOnPrimary">#000000</item>
        <item name="colorControlActivated">#ffffff</item>

        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Binacty.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/boom_background_dark</item>
        <item name="postSplashScreenTheme">@style/Binacty.App</item>
    </style>

    <style name="Binacty.App" parent="Binacty">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>

        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

    <style name="BoomDevButton">
        <item name="android:backgroundTint">@android:color/white</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@android:color/black</item>
    </style>
</resources>